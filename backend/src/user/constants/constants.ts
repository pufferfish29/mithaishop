export const USERNAME_MIN_LENGTH = 3;

export const EMAIL_MIN_LENGTH = 8;

export const PASSWORD_MIN_LENGTH = 8;

export const COLUMN_DEFAULT_LENGTH = 64;

export enum USER_ROLE {
  ADMIN = "admin",
  USER = "user",
  GUEST = "guest",
}
